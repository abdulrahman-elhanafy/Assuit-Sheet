#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int A, B,C,D;
    cin >> A >> B >> C >> D;

    int start = max(A, C);
    int end = min(B, D);

    if (start <= end)
        cout << start << " " << end;
    else
        cout << "-1";
    return 0;

}
