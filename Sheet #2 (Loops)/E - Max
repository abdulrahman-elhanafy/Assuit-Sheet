#include <iostream>
#include <cmath>
using namespace std;
int main() {
    int N;
    cin >> N;
    int mx = -1;
    for (int i = 0; i < N; i++){
        int x;
        cin >> x;
        mx = max(mx, x);
    }
    cout << mx;
    return 0;
}
