#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int A, B ;
    cin >> A >> B;

    int mn;
    mn = min(A, B);

    for (int i = mn; i >= 1; i--){
        if (A%i ==0 && B%i == 0){
            cout << i;
            break;
        }
    }
        return 0;
}
